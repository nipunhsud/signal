{
  "type": "module",
  "name": "trading-agent",
  "version": "2.0.0",
  "description": "Executes graded breakout alerts as broker orders (Alpaca paper first)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc --watch & node --watch dist/index.js",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "tsc && node --test \"dist/**/*.test.js\"",
    "lint": "prettier --check ."
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "dotenv": "^16.0.0",
    "node-cron": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-cron": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
